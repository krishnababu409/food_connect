nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
  .container-fluid
    a.navbar-brand(href='/') Food Connect

    button.navbar-toggler(
      type='button'
      data-bs-toggle='collapse'
      data-bs-target='#donorNav'
    )
      span.navbar-toggler-icon

    #donorNav.collapse.navbar-collapse
      ul.navbar-nav.ms-auto.mb-2.mb-lg-0
        li.nav-item
          a.nav-link(
            href='/dashboard'
            class=(activePath === '/dashboard' ? 'active' : '')
          ) Dashboard

        li.nav-item
          a.nav-link(
            href='/register'
            class=(activePath === '/register' ? 'active' : '')
          ) Add Donation

        li.nav-item
          a.nav-link(
            href='/logout'
          ) Logout
